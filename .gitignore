__pycache__/
*.py[cod]
.ipynb_checkpoints/
# stray filesystem tombstones (created by the FUSE mount when a file is open+deleted)
.fuse_hidden*
# training outputs / model artifacts (don't commit large binaries)
aegis_guard/
hf_upload/
_aegis_src/
*.joblib
*.bin
*.safetensors
# packaging / build / test artifacts
*.egg-info/
build/
dist/
.pytest_cache/
.eggs/
# packaging / local
*.zip
.DS_Store
.env
wandb/
.cache/
# extra model / checkpoint formats (keep large binaries and tuned weights out)
*.pt
*.pth
*.ckpt
*.gguf
*.onnx
*.h5
adapter_model*/
checkpoints/
# datasets / large data (corpora download at runtime; never commit them)
data/raw/
*.parquet
*.npy
*.npz
*.tar
*.tar.gz
# secrets / credentials (patterns chosen NOT to match source like output/secrets.py)
*.pem
*.key
kaggle.json
.env.*
hf_token*
*.token
.secrets/
# private planning docs (local only, do not publish)
RELEASE_AND_IP_PLAN.md
# course/personal artifacts kept local, not in the public repo
presentations/
GITHUB.md
# private research/planning docs (local only)
AEGIS_RESEARCH_ROADMAP.md
